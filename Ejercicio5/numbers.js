const intAltura = 174;
const fAltura = 1.74;
const fPeso = 68.5;

const redonAltura = Math.ceil(fAltura);
const redonPeso = Math.floor(fPeso);

const sonIguales = Number.MAX_VALUE + 1 === Number.MAX_VALUE;

